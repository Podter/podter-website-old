---
import Message, { type MessageData } from "./Message";
import getUser from "@/lib/getUser";

interface Props {
  message: MessageData;
}

const { message } = Astro.props;

const user = await getUser(message.providerAccountId, message.provider);
---

<Message client:visible message={message} user={user} />
